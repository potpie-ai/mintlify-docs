---
title: 'ask_knowledge_graph_queries'
description: 'Executes vector similarity search queries against the knowledge graph to obtain relevant information.'
---

## Usage

The `ask_knowledge_graph_queries` tool allows you to perform vector similarity searches on the knowledge graph. This is useful for finding relevant information based on the semantic meaning of your query.

## Parameters

- `query` (string): The search query to execute against the knowledge graph.

## Example
